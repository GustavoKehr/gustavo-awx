---
#- name: "Executando Storage LVM"
  #ansible.builtin.include_tasks: storage_lvm.yml

#- name: "Executando Pré-requisitos do SO"
  #ansible.builtin.include_tasks: pre_install.yml
  
- name: "Executar tarefas de instalação do DB"
  ansible.builtin.import_tasks: install_db.yml
  tags: [install, libs, clean_inventory, rsp_file, silent_installation, instroot, rootsh]