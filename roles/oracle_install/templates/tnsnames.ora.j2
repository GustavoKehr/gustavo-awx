# tnsnames.ora — managed by Ansible oracle_install role
# Generated: {{ ansible_date_time.iso8601 }}
# SID: {{ oracle_sid }} | Host: {{ ansible_hostname }} | Port: {{ oracle_listener_port }}

{{ oracle_sid }} =
  (DESCRIPTION =
    (ADDRESS = (PROTOCOL = TCP)(HOST = {{ ansible_hostname }})(PORT = {{ oracle_listener_port }}))
    (CONNECT_DATA =
      (SERVER = DEDICATED)
      (SERVICE_NAME = {{ oracle_sid }})
    )
  )

{{ oracle_sid }}_LOCAL =
  (DESCRIPTION =
    (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC{{ oracle_listener_port }}))
    (CONNECT_DATA =
      (SERVER = DEDICATED)
      (SERVICE_NAME = {{ oracle_sid }})
    )
  )
