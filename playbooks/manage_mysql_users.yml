---
- name: "Manage MySQL users"
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: "MySQL: manage user via survey vars"
      ansible.builtin.include_role:
        name: mysql_manage_users
      tags: [mysql, mysql_users]
