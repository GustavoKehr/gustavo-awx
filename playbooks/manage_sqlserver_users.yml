---
- name: "Manage SQL Server users (list-based)"
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: "SQL Server: manage users from db_users list"
      ansible.builtin.include_role:
        name: sql_manage_users
      tags: [sqlserver, sql_users]
