{
  "name": "Oracle 19c Installation Survey",
  "description": "Minimal survey — only the SID is required. All other parameters (passwords, memory, directories, patches) are automatic defaults.",
  "spec": [
    {
      "question_name": "Oracle SID",
      "question_description": "Database SID and name. Also defines the base directory (/oracle/<SID>). Max 8 chars.",
      "required": true,
      "type": "text",
      "variable": "oracle_sid",
      "default": "AWOR",
      "min": 1,
      "max": 8
    }
  ]
}
